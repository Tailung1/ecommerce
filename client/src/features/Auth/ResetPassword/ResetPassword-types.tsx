export type errorTypes = {
  email: string;
  otpCode: string;
  newPassword: string;
  repeatNewPassword: string;
};

export type PhaseTypes = {
  email: boolean;
  otpCode: boolean;
  recovery: boolean;
};


