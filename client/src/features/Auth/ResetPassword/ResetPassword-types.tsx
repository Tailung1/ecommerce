export type errorTypes = {
  userEmail: string;
  otpCode: string;
  newPassword: string;
  repeatNewPassword: string;
};

export type PhaseTypes = {
  userEmail: boolean;
  otpCode: boolean;
  recovery: boolean;
};


